#include<iostream>
using namespace std;
int main()
{
	bool b1(true); // true=1
	bool b2(false); // false = 0
	bool b3=1;      // 1 = true
	bool b4(!true); // b4= not true = false
	bool b5(!false); // b5 not false = true
	bool b6 =!1;      // b6= not 1 = false(0)
	cout<<b1<<"\n";
	cout<<b2<<"\n";
	cout<<!b3<<"\n";
	// moi gia tri # 0 mac dinh la true
#if
std::string strString // toán tử phân giải phạm vi nếu mà ko khai báo thư viện
 string strString( " hoc hoc hoc"); khoi tạo giá trị " học học học " cho chuỗi strString
  nếu nhập chuỗi cin thì ko nhập đc khoảng trắng 
  getline ( cin , strString,"_" ) // hàm nhập chuỗi có khoảng trắng
  "_": nhâp văn bản khi muốn kết thúc dòng thì gọi ra _
#endif	
	
	
	
	
	return 0;
	
}
